<div class="container">
  <div class="articles-box">
    <ul>
      <% @articles.each do |article| %>
      <li><h2><%= link_to "#{article.title}", article_path(article.id), class: 'index-title' %> </h2></li>
      <li><p><%= article.content %> </p></li>
      <% end %>
    </ul>
  </div>
</div>
