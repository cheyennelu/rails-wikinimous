<div class="container">
  <h1><%= @article.title %></h1>
  <p><%= link_to 'Edit', edit_article_path %>
    <%= link_to 'Delete', article_path(@article.id), method: :delete, data: { confirm: "Delete #{@article.title}?" } %></p>
  <p><%= @article.content %> </p>
</div>
